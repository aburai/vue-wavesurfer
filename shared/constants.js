export const DEFAULT_CONFIG = {
  height: 48,
  size: 12,
  center: true,
  splitChannels: false,
  muted: false,
  peaks: false,
  customRenderer: false,
  interact: true,
  dragSelection: true,
  canEdit: false,
  drawType: 'default',
  gradient: true
}

export const DEFAULT_CALLBACKS = {
  onLoadAction: () => {},
  onPlayAction: () => {}
}
